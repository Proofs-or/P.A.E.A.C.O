    #include <stdio.h>

    /*void pu_calcbases(double Sbase, double Vbase, Bases *b, int zonas, int zVbase){
        
        for(int  i = 0; i < zonas; i++){
            (b + sizeof(b)*i)->V = Vbase;
            (b + sizeof(b)*i)->S = Sbase;
            (b + sizeof(b)*i)->Z = (b + sizeof(b)*i)->V * (b + sizeof(b)*i)->V / (b + sizeof(b)*i)->S;
            (b + sizeof(b)*i)->I = 2; //Sei n, ajuda nois
        }
    }*/

    /*void comporzona(int* inicio, Componente c, int ncomp, int* z, int nz){

        int aux = ncomp + 2;
        for(int i = 0; i < ncomp; i++){
            if((c + sizeof(c)*i).pos[0] == inicio){
                aux = i;
                break;
            }
        }
        if(aux > ncomp){
            printf("Error -> comporzona")
        }else{
            while(1){
                for
            }
        }

    }*/